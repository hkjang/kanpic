## 릴리즈 요약

v0.196.1은 v0.196.0에서 CI를 빨갛게 만든 **gofmt** 어긋남을 고칩니다.

## 여기서는 보이지 않는 것

폭포 시험 파일의 구조체 자리맞춤이 gofmt 와 달랐습니다. `go build` 도 `go test` 도 통과하므로 **로컬에서는 아무 표시가 나지 않고** CI의 `gofmt` 검사에서만 걸립니다.

파이썬으로 글자를 끼워 넣어 Go 파일을 고치면 이런 일이 납니다. 넣은 뒤에는 `gofmt` 를 돌려야 합니다.

기능은 그대로입니다.
