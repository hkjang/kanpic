## 릴리즈 요약

v0.141.0은 **수학 함수 29개** 를 더합니다. 구글 시트가 아는 함수 400개 중 kanpic이 아는 것이 **278개에서 307개** 로 늘었습니다.

## 삼각의 역수와 쌍곡선

`SEC`, `CSC`, `COT`, `SECH`, `CSCH`, `COTH`, `ACOT`, `ACOTH`, `ACOSH`, `ASINH`, `ATANH`, `GAMMALN`, `SQRTPI` 가 없었습니다.

나누는 쪽이 0이면 **무한대 대신 `#DIV/0!`** 을 냅니다. 무한대를 내면 뒤의 셈이 조용히 망가집니다.

`ACOT` 은 엑셀과 같이 **0과 파이 사이** 의 값을 냅니다. `atan(1/x)` 로 적으면 음수에서 부호가 뒤집혀 `ACOT(-1)` 이 `-0.785398` 이 됩니다. 올바른 답은 `2.356194` 입니다.

## 자리 올림은 음수에서 갈립니다

```
CEILING.MATH(-5.5)        ->  -5    0 쪽으로 올린다(기본)
CEILING.MATH(-5.5, 1, 1)  ->  -6    0 에서 멀어지는 쪽으로
FLOOR.MATH(-5.5)          ->  -6    아래로 내린다(기본)
FLOOR.MATH(-5.5, 1, 1)    ->  -5    0 쪽으로
CEILING.PRECISE(-4.3)     ->  -4    언제나 위로, 기준의 부호는 무시
FLOOR.PRECISE(-3.2)       ->  -4    언제나 아래로
```

양수에서는 넷이 모두 같은 답을 내므로, 하나로 뭉뚱그려도 한동안 티가 나지 않습니다. 그러다 음수에서 **조용히** 다른 값을 냅니다. 그래서 갈래마다 따로 구현하고 음수 자리를 열여섯 가지로 나누어 확인했습니다.

## 조합, 제곱합, 진법

`COMBINA`(되풀이를 허용한 조합), `FACTDOUBLE`(이중 계승), `MULTINOMIAL`, `SUMX2MY2`, `SUMX2PY2`, `SUMXMY2`, `SERIESSUM` 을 더했습니다. `BASE` 와 `DECIMAL` 로 2~36진법을 오갑니다.

`MUNIT` 은 단위 행렬을, `RANDARRAY` 는 지정한 크기의 난수를 펼칩니다. `RANDARRAY` 는 값이 매번 달라지므로 **다시 계산하는 함수** 목록에도 넣었습니다 — 넣지 않으면 한 번 뽑은 값이 그대로 굳습니다.

## 남은 것

400개 중 **93개** 가 아직 없습니다. 통계 분포 39개, 재무 22개, 웹 연동 10개가 큰 덩어리입니다.

## 검증 결과

- 값은 손으로 셀 수 있는 것으로 고릅니다. `COMBINA(4,3)=C(6,3)=20`, `FACTDOUBLE(6)=6×4×2=48`, `SUMXMY2({2,3,9},{6,5,11})=16+4+4=24` 입니다.
- 답할 수 없는 아홉 자리(0으로 나누기, 정의역 밖)를 함께 봅니다.
- Go 시험 전체, 통합 시험 전체와 웹 시험 382개가 통과합니다.
