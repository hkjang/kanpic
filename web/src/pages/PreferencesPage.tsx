import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { useState } from 'react'
import { ArrowLeft, Check, Copy, Eye, KeyRound, Monitor, Moon, Palette, Plus, RotateCw, Settings, Sun, Trash2, UserRound } from 'lucide-react'
import { AppHeader } from '../components/AppHeader'
import { api } from '../lib/api'
import type { ApiKey, BuildInfo, CreatedApiKey, Session } from '../types'

export function PreferencesPage({build,session}:{build?:BuildInfo;session?:Session}){const [section,setSection]=useState<'appearance'|'keys'>('appearance');return <div className="page-shell preferences-shell"><AppHeader build={build} session={session}/><main className="preferences-layout"><aside><a href="/"><ArrowLeft/> 워크스페이스</a><div className="preference-person"><span>{(session?.user?.display_name||'로컬').slice(0,1)}</span><div><strong>{session?.user?.display_name||'로컬 관리자'}</strong><small>{session?.user?.email||'개인 설정'}</small></div></div><nav><button className={section==='appearance'?'active':''} onClick={()=>setSection('appearance')}><Palette/> 개인화</button><button className={section==='keys'?'active':''} onClick={()=>setSection('keys')}><KeyRound/> API 키</button></nav></aside>{section==='appearance'?<AppearancePanel/>:<ApiKeysPanel/>}</main></div>}
function AppearancePanel(){const preferences=useQuery({queryKey:['preferences'],queryFn:()=>api<{values:Record<string,unknown>}>('/api/v1/me/preferences')});const [theme,setTheme]=useState('system'),[locale,setLocale]=useState('ko-KR'),[density,setDensity]=useState('comfortable');const save=useMutation({mutationFn:()=>api('/api/v1/me/preferences',{method:'PUT',body:JSON.stringify({values:{...preferences.data?.values,theme,locale,density}})})});return <section className="preference-content"><span className="eyebrow">PERSONALIZATION</span><h1>나만의 작업 환경</h1><p>이 설정은 현재 계정에만 적용되며 시스템 설정과 분리됩니다.</p><div className="preference-card"><div className="preference-heading"><Monitor/><div><h2>화면 테마</h2><p>kanpic 화면의 밝기를 선택하세요.</p></div></div><div className="theme-options">{[['light',Sun,'라이트'],['dark',Moon,'다크'],['system',Monitor,'시스템 설정']].map(([value,Icon,label])=><button className={theme===value?'active':''} onClick={()=>setTheme(value as string)} key={value as string}><span><Icon/></span><strong>{label as string}</strong>{theme===value&&<Check/>}</button>)}</div></div><div className="preference-card rows"><label><div><Settings/><span><strong>언어 및 로케일</strong><small>날짜와 숫자 표시 방식</small></span></div><select value={locale} onChange={e=>setLocale(e.target.value)}><option value="ko-KR">한국어 (대한민국)</option><option value="en-US">English (United States)</option></select></label><label><div><Eye/><span><strong>화면 밀도</strong><small>도구와 목록의 간격</small></span></div><select value={density} onChange={e=>setDensity(e.target.value)}><option value="comfortable">여유롭게</option><option value="compact">조밀하게</option></select></label></div><button className="primary save-preference" onClick={()=>save.mutate()}>{save.isSuccess?<Check/>:null} 개인 설정 저장</button></section>}
function ApiKeysPanel(){const client=useQueryClient();const [secret,setSecret]=useState(''),[name,setName]=useState('새 에이전트 키'),[showCreate,setShowCreate]=useState(false);const keys=useQuery({queryKey:['my-api-keys'],queryFn:()=>api<{items:ApiKey[]}>('/api/v1/me/api-keys')});const create=useMutation({mutationFn:()=>api<CreatedApiKey>('/api/v1/me/api-keys',{method:'POST',body:JSON.stringify({name,scopes:['mcp.use','workbook.*','range.*','formula.*','version.*','profile.read']})}),onSuccess:item=>{setSecret(item.secret);setShowCreate(false);client.invalidateQueries({queryKey:['my-api-keys']})}});const rotate=async(id:string)=>{if(!confirm('기존 키는 즉시 폐기됩니다. 회전할까요?'))return;const item=await api<CreatedApiKey>(`/api/v1/me/api-keys/${id}:rotate`,{method:'POST',body:'{}'});setSecret(item.secret);client.invalidateQueries({queryKey:['my-api-keys']})};const revoke=async(id:string)=>{if(!confirm('이 키를 폐기할까요?'))return;await api(`/api/v1/me/api-keys/${id}`,{method:'DELETE'});client.invalidateQueries({queryKey:['my-api-keys']})};return <section className="preference-content"><div className="preference-title-row"><div><span className="eyebrow">DEVELOPER ACCESS</span><h1>개인 API 키</h1><p>REST와 MCP 에이전트에서 사용할 최소 권한 키를 관리합니다.</p></div><button className="primary" onClick={()=>setShowCreate(true)}><Plus/> 새 키</button></div>{secret&&<div className="secret-banner"><KeyRound/><div><strong>새 키를 지금 복사하세요</strong><p>보안을 위해 이 원문은 다시 표시되지 않습니다.</p><code>{secret}</code></div><button onClick={()=>navigator.clipboard.writeText(secret)}><Copy/> 복사</button></div>}<div className="key-list">{keys.data?.items.map(key=><div className={`key-card ${key.revoked_at?'revoked':''}`} key={key.id}><div className="key-icon"><KeyRound/></div><div className="key-info"><div><strong>{key.name}</strong><span className={key.revoked_at?'disabled-badge':'enabled-badge'}>{key.revoked_at?'폐기':'활성'}</span></div><code>{key.prefix}••••••••••••</code><div className="scope-list">{key.scopes.map(scope=><span key={scope}>{scope}</span>)}</div><small>생성 {new Date(key.created_at).toLocaleDateString('ko-KR')} · {key.last_used_at?`최근 사용 ${new Date(key.last_used_at).toLocaleString('ko-KR')}`:'아직 사용하지 않음'}</small></div>{!key.revoked_at&&<div className="key-actions"><button onClick={()=>rotate(key.id)}><RotateCw/> 회전</button><button className="danger" onClick={()=>revoke(key.id)}><Trash2/> 폐기</button></div>}</div>)}</div>{showCreate&&<div className="modal-backdrop"><div className="modal"><h2>새 API 키</h2><p>기본값은 에이전트가 워크북을 읽고 편집하는 데 필요한 scope입니다.</p><label>키 이름<input value={name} onChange={e=>setName(e.target.value)}/></label><div className="scope-preview"><span>mcp.use</span><span>workbook.*</span><span>range.*</span><span>formula.*</span><span>version.*</span></div><div className="modal-actions"><button className="secondary" onClick={()=>setShowCreate(false)}>취소</button><button className="primary" onClick={()=>create.mutate()}>키 생성</button></div></div></div>}</section>}
